// s02-top
// Define the top-type witness S₂ for the diverse schema set.

import { top } from "@typecarta/core";
import type { TypeTerm } from "@typecarta/core";

/** S₂ — Top (primary witness: π₂). ⟦S₂⟧ = 𝒱 */
export const S02_TOP: TypeTerm = top();
