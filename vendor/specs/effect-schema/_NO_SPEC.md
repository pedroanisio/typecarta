# Effect Schema 0.x — no formal spec

Effect Schema is a TypeScript library, not a standardized format. There is no spec separate from the library's source.

Closest authoritative reference: the Effect-TS/effect repository and its docs (https://effect.website/docs/schema/introduction/). Pinning a minor version recommended for any concrete claim.

This file is generated by `scripts/download-specs.py`.
